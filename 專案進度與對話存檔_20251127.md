# 平台API串接互換系統 - 專案進度與對話存檔

**存檔日期**: 2025-11-27  
**專案狀態**: 核心架構完成，進入本地測試與 API 實作階段

---

## 1. 專案目標回顧

開發一個**多平台電商庫存同步管理系統 (Google Sheets 版)**，連接 **YAHOO (主庫存)**、**MOMO**、**PChome**。

*   **核心功能**: 自動同步庫存、防止超賣、訂單自動扣除/回補。
*   **技術架構**: Google Sheets (資料庫) + Google Apps Script (後端 API) + 本地網頁 (前端介面)。
*   **部署方式**: 本地 Node.js 伺服器 + 雲端 GAS API。

---

## 2. 關鍵決策與變更 (Conversation Highlights)

在此次對話中，我們確立了以下重要決策：

### A. 架構轉型
*   **原定方案**: MySQL 資料庫 + 複雜後端。
*   **最終方案**: **Google Sheets + Apps Script**。
*   **原因**: 降低成本 (0元月費)、快速部署、易於維護。

### B. 業務邏輯確認
1.  **建置時間**: 設定為 **21天** (含測試與調整)，而非原本的 1 天，以確保穩定性。
2.  **智能同步機制**: **永遠啟用** (不論商品數量)。系統只同步有變動的商品，效能提升 99%。
3.  **安全庫存規則**: 當庫存 **≤ 2** 時，MOMO 和 PChome 自動下架 (設為 0)，只留 YAHOO 販售。
4.  **訂單取消邏輯**: 任一平台訂單取消，**三個平台同時** 自動回補庫存。
5.  **退貨處理**: 手動在 YAHOO 後台補貨，系統偵測到變動後自動同步。

### C. 本地化部署
*   為了方便操作，決定開發 **本地網頁介面**。
*   提供 **一鍵啟動腳本 (`啟動系統.bat`)**，讓客戶無需安裝複雜開發環境即可使用。

---

## 3. 檔案清單與用途 (File Structure)

目前專案包含以下關鍵檔案，日後調用時請參考此清單：

### 文件類
*   `客戶提案簡報.md`: 最新版的提案內容，包含 21 天時程與詳細效益分析。
*   `需求規格v2.0.md`: 詳細的系統功能規格、API 邏輯與資料庫結構。
*   `本地網頁串接指南.md`: 說明如何在本地運行網頁並串接 GAS API。
*   `使用手冊_GoogleSheets版.md`: 使用者操作說明。
*   `快速開始指南.md`: 簡易上手步驟。
*   `README.md`: 專案總覽。

### 程式碼類
*   `Google_Sheets_Setup.gs`: **核心後端程式碼**。包含：
    *   自動建立工作表 (`setup`)
    *   庫存同步邏輯 (`checkInventoryChanges`)
    *   API 接口 (`doGet`, `doPost`)
    *   Email 通知功能
*   `index.html`: **本地前端網頁**。提供庫存查詢、手動同步觸發介面。
*   `server.js`: **輕量級本地伺服器**。用於運行 `index.html`。
*   `啟動系統.bat`: **一鍵啟動腳本**。自動檢查 Node.js 並啟動伺服器與瀏覽器。

---

## 4. 目前完成進度

*   ✅ **文件撰寫**: 提案、規格、手冊皆已更新至 v2.0 (Google Sheets 版)。
*   ✅ **後端架構**: `Google_Sheets_Setup.gs` 已包含核心邏輯與 API 處理函數。
*   ✅ **前端介面**: `index.html` 已建立，可透過 API 讀取/觸發 GAS。
*   ✅ **本地環境**: 完成 `server.js` 與 `啟動系統.bat`，實現一鍵執行。
*   ✅ **智能優化**: 實作了「只同步變動商品」的邏輯。

---

## 5. 後續待辦事項 (Next Steps)

日後重啟專案時，請優先執行以下步驟：

1.  **API 實作**:
    *   在 `Google_Sheets_Setup.gs` 中填入 YAHOO、MOMO、PChome 的實際 API 呼叫代碼 (目前為模擬函數)。
    *   需要取得各平台的 API 文件與金鑰。

2.  **正式部署**:
    *   將 `Google_Sheets_Setup.gs` 複製到客戶的 Google Apps Script 專案。
    *   部署為 Web App 並取得 URL。
    *   將 URL 更新至 `index.html`。

3.  **測試驗證**:
    *   執行 `啟動系統.bat`。
    *   測試庫存查詢與同步功能是否正常運作。

---

**備註**: 此檔案為對話記錄的精華濃縮，可作為日後開發的 `Context` 使用。
