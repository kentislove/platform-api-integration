# 本地網頁串接 Google Apps Script 指南

要在本地電腦上運行網頁並串接 Google Apps Script (GAS) 及平台 API，您需要克服瀏覽器的安全性限制 (CORS)。以下是完整的實作步驟：

## 核心架構

```
[本地網頁 (Frontend)]
       ↓ (HTTP Request)
[Google Apps Script (Backend API)] ← 儲存 API 金鑰 (安全)
       ↓ (UrlFetchApp)
[Google Sheets (Database)] + [平台 API (Yahoo/Momo/PChome)]
```

> **為什麼不直接從網頁呼叫平台 API？**
> 1. **安全性**: 直接在網頁呼叫會暴露您的 API 金鑰。
> 2. **CORS 問題**: 許多平台 API 不允許從瀏覽器直接跨網域呼叫。
> 3. **最佳解法**: 讓 GAS 當作「中介層 (Proxy)」，網頁只跟 GAS 溝通。

---

## 步驟一：修改 Google Apps Script (後端)

您需要在 `Google_Sheets_Setup.gs` 中加入 `doGet` 和 `doPost` 函數，讓它變成一個 API 伺服器。

### 1. 新增 API 處理函數

請將以下程式碼加入 `Google_Sheets_Setup.gs` 的最下方：

```javascript
// ============================================
// API 服務接口 (供本地網頁呼叫)
// ============================================

function doGet(e) {
  return handleApiRequest(e);
}

function doPost(e) {
  return handleApiRequest(e);
}

function handleApiRequest(e) {
  // 處理 CORS (允許跨網域存取)
  // 注意: GAS 的 Web App 部署後會自動處理部分 CORS，但我們回傳 JSON 時需標準化
  
  var action = e.parameter.action;
  var result = {};
  
  try {
    if (action === 'getInventory') {
      // 範例: 取得庫存
      var vendorCode = e.parameter.code;
      result = getInventoryData(vendorCode);
    } else if (action === 'syncInventory') {
      // 範例: 觸發同步
      checkInventoryChanges();
      result = { status: 'success', message: '同步已觸發' };
    } else {
      result = { status: 'error', message: '未知動作' };
    }
  } catch (error) {
    result = { status: 'error', message: error.toString() };
  }
  
  return ContentService.createTextOutput(JSON.stringify(result))
    .setMimeType(ContentService.MimeType.JSON);
}

// 輔助函數: 取得庫存資料
function getInventoryData(code) {
  const ss = SpreadsheetApp.getActiveSpreadsheet();
  const sheet = ss.getSheetByName(CONFIG.SHEETS.INVENTORY_MASTER);
  const data = sheet.getDataRange().getValues();
  
  // 簡單搜尋邏輯
  for (var i = 1; i < data.length; i++) {
    if (data[i][1] == code) {
      return {
        status: 'success',
        data: {
          code: data[i][1],
          qty: data[i][2],
          updated: data[i][5]
        }
      };
    }
  }
  return { status: 'error', message: '找不到商品' };
}
```

### 2. 部署為網頁應用程式

1. 在 Apps Script 編輯器右上角點擊 **「部署」** -> **「新增部署作業」**。
2. 類型選擇 **「網頁應用程式」**。
3. 說明：`API v1`。
4. 執行身分：**「我」** (Me)。
5. **誰可以存取**：**「所有人」** (Anyone)。
   * **注意**: 這是讓本地網頁能存取的關鍵。
6. 點擊 **「部署」**。
7. **複製「網頁應用程式網址」** (以 `/exec` 結尾的網址)。

---

## 步驟二：建立本地網頁 (前端)

在您的電腦上建立一個資料夾，裡面放一個 `index.html`。

### `index.html` 範例

```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>庫存管理系統</title>
    <style>
        body { font-family: sans-serif; padding: 20px; }
        .card { border: 1px solid #ccc; padding: 15px; margin-top: 10px; border-radius: 5px; }
        button { padding: 10px 20px; cursor: pointer; }
    </style>
</head>
<body>
    <h1>平台庫存管理系統</h1>
    
    <div class="card">
        <h3>庫存查詢</h3>
        <input type="text" id="vendorCode" placeholder="輸入廠商原料號">
        <button onclick="searchInventory()">查詢</button>
        <div id="result"></div>
    </div>

    <div class="card">
        <h3>系統操作</h3>
        <button onclick="triggerSync()">手動觸發同步</button>
        <div id="syncStatus"></div>
    </div>

    <script>
        // 填入您部署後的 GAS 網址
        const GAS_API_URL = '您的_GAS_WEB_APP_URL';

        async function searchInventory() {
            const code = document.getElementById('vendorCode').value;
            const resultDiv = document.getElementById('result');
            resultDiv.innerHTML = '查詢中...';

            try {
                // 使用 fetch 呼叫 GAS API
                // 注意: GAS 會重新導向，所以要跟隨 redirect
                const response = await fetch(`${GAS_API_URL}?action=getInventory&code=${code}`, {
                    method: 'GET',
                    mode: 'cors' // 重要: 啟用跨域
                });
                
                const data = await response.json();
                
                if (data.status === 'success') {
                    resultDiv.innerHTML = `
                        <p>商品: ${data.data.code}</p>
                        <p>庫存: <b>${data.data.qty}</b></p>
                        <p>更新: ${new Date(data.data.updated).toLocaleString()}</p>
                    `;
                } else {
                    resultDiv.innerHTML = `<span style="color:red">${data.message}</span>`;
                }
            } catch (e) {
                resultDiv.innerHTML = '連線錯誤: ' + e.message;
            }
        }

        async function triggerSync() {
            const statusDiv = document.getElementById('syncStatus');
            statusDiv.innerHTML = '同步請求發送中...';
            
            try {
                // 使用 POST 也可以，但在 GAS 簡單實作中 GET 較方便測試
                const response = await fetch(`${GAS_API_URL}?action=syncInventory`, {
                    method: 'POST', // 這裡示範 POST
                    mode: 'no-cors' // GAS 的 POST 有時會有 CORS 限制，no-cors 是一種繞過方式，但拿不到回應內容
                    // 如果需要回應，建議統一用 GET 帶參數，或在 GAS 處理 OPTIONS preflight
                });
                
                // 注意: mode: 'no-cors' 拿不到 JSON 回傳，只能確認請求已發出
                statusDiv.innerHTML = '同步指令已發送 (背景執行中)';
            } catch (e) {
                statusDiv.innerHTML = '錯誤: ' + e.message;
            }
        }
    </script>
</body>
</html>
```

---

## 步驟三：在本地運行 (關鍵)

**不能直接雙擊 `index.html` 開啟！** (網址是 `file:///...` 會被瀏覽器擋下 API 請求)。
您必須透過「本地伺服器」來開啟。

### 方法 A: 使用 VS Code (推薦)
1. 安裝擴充套件 **"Live Server"**。
2. 在 `index.html` 上按右鍵 -> **"Open with Live Server"**。
3. 瀏覽器會自動開啟 `http://127.0.0.1:5500/index.html`。

### 方法 B: 使用 Python (如果您有安裝)
1. 開啟終端機 (CMD/PowerShell)。
2. 進入 `index.html` 所在的資料夾。
3. 輸入 `python -m http.server`。
4. 瀏覽器開啟 `http://localhost:8000`。

---

## 常見問題

1. **CORS 錯誤**: 
   - 確保 GAS 部署為 **"Anyone"** (所有人)。
   - 確保使用 `http://localhost` 或 `http://127.0.0.1` 開啟網頁，而不是 `file://`。

2. **POST 請求拿不到回應**:
   - Google Apps Script 的 `doPost` 在跨域時處理較複雜。
   - **簡單解法**: 全部改用 `doGet` (GET 請求)，把參數放在網址列 (如 `?action=update&qty=10`)，這樣前端可以用 `await response.json()` 正常拿到資料。

3. **安全性**:
   - 雖然 GAS 網址是公開的，但您可以在 GAS 程式碼中檢查傳入的參數 (例如加一個簡單的 `&token=密碼`) 來做基本驗證。
